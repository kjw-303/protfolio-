// 이 파일은 하위 호환을 위해 유지됩니다. 직접 사용하지 마세요.
export { DEFAULT_PROJECTS } from './defaultProjects.js';
export { getProjectsFromCache as getProjects, fetchProjects, saveProjects, getProjectById } from '../services/projectService.js';
